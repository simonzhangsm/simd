{{title|SIMD_HAS_INT16_SHIFT_R_BY_VECTOR}}
{{cap/navbar}}
{{dcl begin}}
{{dcl |
#define SIMD_HAS_INT16_SHIFT_R_BY_VECTOR /* 1 if present, 0 otherwise */
}}
{{dcl end}}

Indicates support of efficient right per-element shift of {{c|int16}} vector data (see {{ltf|int/shift_r_v | shift_r}}). The macro evaluates to {{c|1}} if this operation is supported in the SIMD unit, {{c|0}} otherwise.

Currently, the macro is defined to {{c|1}} on the following instruction sets:

* {{c|SIMD_USE_NULL}}
* {{c|SIMD_USE_AVX512BW}}
* {{c|SIMD_USE_NEON}}
* {{c|SIMD_USE_ALTIVEC}}
* {{c|SIMD_USE_MSA}}
